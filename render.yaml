services:
  - type: web
    name: school-backend
    runtime: node
    buildCommand: npm ci && npm rebuild sqlite3 --build-from-source
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
